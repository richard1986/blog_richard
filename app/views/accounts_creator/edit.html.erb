
			<% unless current_user.account.id == @account.id %>
				<h1 class="page-header"><p class="text-center">Editing Account</p></h1>
				<% else %>
				<h1 class="page-header"><p class="text-center">Editing My Profile</p></h1>	
			<% end %>

			<%= render 'form' %>

			<%= link_to '<span class="glyphicon glyphicon glyphicon-sunglasses"></span>&nbsp;Show'.html_safe, @account, class: "btn btn-info" %>
			<%= link_to '<span class="glyphicon glyphicon-chevron-left"></span>&nbsp;Back'.html_safe, :back, class: "btn btn-danger" %>
			
			</div>
		</div>
	</div>
</div>
